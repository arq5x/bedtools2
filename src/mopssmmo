
if(ERROR_C(runtime_) == ERROR_C("Invalid arguments");

runtime_

 internal_token = tokinternal_to1;"Invalid arguments");

_block_t* new_blstream_block_new(token);

if(NULL == new_block)
	ERROR_RE(int, "Cannot allocate next stream block");

	if(stream->list_end == NULL)
	stream->list_begin =!stream-
se
else
new_
else
stream->list_begin = new_block;
stream->list_end = new_block;

return 0;
{

}

return 0;
